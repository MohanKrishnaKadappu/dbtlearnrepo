17:06:38  Profile dbt_training written to C:\Users\UJ921DJ\.dbt\profiles.yml using target's profile_template.yml and your supplied values. Run 'dbt debug' to validate the connection.


.venv\scripts\activate


to run single model  dbt run --select final_expense_claims.sql
or
dbt run -s final_expense_claims

to run a parent along with downstream dependencies [child]

dbt run --select stg_expense_claims+

to run upstream along with upstream dependencies [child]

dbt run --select +final_expense_claims


to run at folder level - all models
dbt run --select models/staging   

--to not run a model but all models
dbt run --exclude my_first_dbt_model



--question
--significance of threads in profiles.yml  -- 3 threads

----question 
--whatever i have in staging folder models shouldnt be visible


--materialization
--what if I need you keep permanent table , view - [secure,materialized],dynmaic table
--then go to profiles.yml/dbt_project.yml +materilized: view command
--you can update it
--example - {{ config(materialized='table',transient=false) }}  --for permanent table
-- similary for secure view 
{{config(materialized='view',secure=true)}}


--we shall define sources in schema.yml
--schema name of file is not standardized


--incremental strategies
--in dbt we have 4 strategies
--append
--merge
--insertt+overwrite
---delete-insertt


--run full folder 

 dbt run --select models/dynamic_tables


 https://cloudyard.in/2025/08/building-iceberg-tables-in-snowflake-macros-vs-native-support/


 version: 2

sources:
    - name: practice
      database: DBT_PRACTICE
      schema: PUBLIC
      tables:
        - name: customers
          description: "Staging table for customer data"
        - name: orders
          description: "Staging table for orders data"

--first generate dbt docs

 dbt docs generate 

 then to serve 

 dbt docs serve



 ----------------------------------

 Praveen dbt cloudard

 To save model with your own name instead of sql 
 use alias = 'name' 
--model/demo.sql
{{
    config(
        materialized='table',
        transient=false,
        alias='Mohan_test',
        query_tag='trails'
    )
}}

SELECT 1 ID

---dbt shortcuts 
give underscore underscore then whatever as per your need 

__ref 
__conf

--to execute upstream models 

dbt run -s +e2_stg

--I want to run all these models parallely 

dbt run -s e2_core e2_stg s2

--to execute upstream models amd downstream models

dbt run -s +e2_stg+

The --exclude flag allows you to omit certain models or directories from the run.
dbt run --exclude staging.my_excluded_model


Running with specific tags or configurations:
  dbt run --select tag:my_tag
  dbt run --select config:materialized:incremental

Full refresh of incremental models:
  dbt run --select my_incremental_model --full-refresh

  --to run all models , in models folder 
  dbt run 

  --to run folde 
  dbt run -s dynamic 


  --question 
  Can I store my models in a directory other than the default directory in my project 
  --yes just add path then update in models path

  model-paths: ["models","testing_outside_model"]

  to run seed 

  dbt seed -s file_name.csv


  --small data / static data 
  full truncate n load 

  dbt seed --full refresh -s seed_name 

--first run the models then  

--run tests 
dbt test -s customers orders


--other way - update schema.yml 
dbt test -s source:rawsource
rawsource